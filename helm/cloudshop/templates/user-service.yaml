apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: {{ .Values.namespace }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: user-service 

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001

