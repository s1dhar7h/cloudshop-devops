apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: {{ .Values.namespace }}

spec:
  ttype: {{ .Values.service.type }}

  selector:
    app: notification-service 

  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004


      
