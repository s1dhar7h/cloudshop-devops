apiVersion: apps/v1
kind: Deployment

metadata:
  name: order-service
  namespace: cloudshop

spec:
  replicas: 2

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: order-service

  template:
    metadata:
      labels:
        app: order-service

    spec:
      containers:
        - name: order-service
          image: s1dhar7h/cloudshop-order-service:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: order-service-config
            - configMapRef:
                name: database-config
            - secretRef:
                name: database-secret
          ports:
          - containerPort: 5003
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 5003
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 5003
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
