apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: cloudshop

spec:
  type: ClusterIP

  selector:
    app: order-service 

  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
