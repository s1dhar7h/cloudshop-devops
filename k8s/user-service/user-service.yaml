apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: cloudshop

spec:
  type: ClusterIP

  selector:
    app: user-service 

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
