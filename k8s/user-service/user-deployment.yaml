apiVersion: apps/v1
kind: Deployment

metadata:
  name: user-service
  namespace: cloudshop

spec:
  replicas: 2

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: user-service

  template:
    metadata:
      labels:
        app: user-service

    spec:
      containers:
        - name: user-service
          image: s1dhar7h/cloudshop-user-service:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: user-service-config
            - configMapRef:
                name: database-config
            - secretRef:
                name: database-secret
          ports:
          - containerPort: 5001
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 5001
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 5001
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
